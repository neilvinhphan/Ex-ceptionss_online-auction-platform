# OOP_JAVA_PRJ

## Maven Wrapper

You can build and test the project without installing Maven globally:

```bash
# Windows
mvnw.cmd clean test

# macOS / Linux
./mvnw clean test
```

The wrapper still requires Java to be installed.
